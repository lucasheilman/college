#include <string>
#include <iostream>
using namespace std;

string urlify(string str1){
  for(int i = 0; i<str1.length();i++){
    if(str[i] == " " ){
      str1.replace(i,1,"%20");
    }
  }
  return str1;
}

int main(){
  string str = "this is a string";
  cout << urlify(str) << endl;
}
